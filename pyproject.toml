[project]
name = "ai-portal"
version = "0.1.0"
requires-python = ">=3.13"
description = "Family AI Portal launcher and chat proxy"

[tool.ruff]
target-version = "py313"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
